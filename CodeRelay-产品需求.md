# CodeRelay 产品需求文档

> 文档状态：持续细化中  
> 产品名称：`CodeRelay`  
> 目标平台：Windows 桌面端  
> 设计基准：约 `1200 × 760`  
> 标记说明：**已确认**为当前需求；**建议**为基于 Apple Design Skills 和 Windows 桌面端习惯提出的设计建议；**待确认**为尚未最终决定的产品行为。

### 本轮已确认

- 视觉方向采用“macOS 风味、Windows 行为”；
- 悬停导航同时支持点击固定打开、键盘焦点打开和 `Escape` 关闭；
- 账号池主体采用高密度表格或可展开列表，顶部保留少量汇总卡片；
- CodeBuddy 账号添加采用统一面板，支持浏览器认证、手动 Token 和配置文件导入；
- 导入账号先预览，再确认写入，并展示成功、重复和失败结果；
- 动效用于反馈和空间关系，不使用装饰性循环、夸张弹跳或阻塞式动画；
- 透明和模糊主要用于标题栏、导航浮层、菜单和非阻塞详情面板，主内容区保持清晰；
- 服务配置页采用概要区与分组表单，不把所有字段铺成一个长表单；
- macOS 风味通过材质、空间、排版、圆角和动效节奏体现，不照搬 macOS 的窗口控件和平台行为；
- 支持减少动效、减少透明、高对比度和键盘操作。

---

## 1. 产品概述

CodeRelay 是面向高级用户的 Windows 桌面工具，用于管理 CodeBuddy 账号池、模型能力和本地反代服务，并对请求进行账号调度、统计和日志记录。

产品风格参考 Raycast、Cursor 等开发者工具：信息密度较高、操作路径明确、界面克制，不使用装饰性视觉元素干扰配置和监控任务。

CodeRelay 的核心工作方式是：

1. 用户在 CodeRelay 中添加并管理多个 CodeBuddy 账号；
2. 用户配置本地反代服务和 API Key；
3. 用户手动启动反代服务；
4. CodeRelay 根据 API Key 绑定范围和调度策略选择 CodeBuddy 账号；
5. 用户通过 OpenAI 兼容接口访问服务，并在 CodeRelay 中查看运行状态、统计数据和请求日志。

反代服务不随软件启动自动运行。软件启动后默认进入总览页，用户需要明确执行启动操作。

### 1.1 产品目标

- 让高级用户能够集中管理多个 CodeBuddy 账号；
- 通过账号池提高请求可用性和调度灵活性；
- 提供清晰可控的本地 OpenAI 兼容反代服务；
- 让用户能够快速判断服务是否健康、请求是否成功、账号是否可用；
- 在不牺牲信息密度的情况下，保持界面稳定、简洁和可预测。

### 1.2 非目标

- 不在软件启动时自动启动反代服务；
- 不强制用户采用单一账号认证方式；
- 不把完整本地连接信息放在总览页长期暴露；
- 不把所有高级配置同时展示在首屏；
- 不直接修改或复用旧项目的实现文件。旧项目和旧截图只作为视觉与功能参考。

---

## 2. 用户与典型场景

### 2.1 目标用户

- 熟悉 API、模型和本地服务配置的开发者；
- 需要在多个 CodeBuddy 账号之间进行请求调度的高级用户；
- 需要将本地反代接入 Cursor、命令行工具或其他 OpenAI 兼容客户端的用户。

### 2.2 典型场景

#### 场景 A：首次配置

用户打开 CodeRelay，进入账号池，选择 OAuth/网页登录、手动粘贴 Token 或导入已有账号配置文件中的一种方式添加账号，然后在 API Key 页面创建 Key，最后进入服务配置页启动服务。

#### 场景 B：服务运行监控

用户打开软件后进入总览页，查看服务状态、可用账号数量、请求量、缓存命中情况和调度状态。服务停止后，总览保留最近一次运行数据，并明确标注数据所属的运行时间范围。

#### 场景 C：账号失效处理

某个账号 Token 失效或连续请求失败，账号池显示需要处理状态。用户进入账号详情重新认证或更新 Token，不影响其他账号继续参与调度。

#### 场景 D：关闭窗口

用户点击关闭按钮时，如果反代正在运行，CodeRelay 询问用户是最小化到系统托盘，还是停止服务后退出。用户可以将选择保存为后续默认行为。

---

## 3. 信息架构与导航

### 3.1 一级导航

- 总览
- 反代服务
- CodeBuddy
- 设置

### 3.2 二级导航

#### 反代服务

- 服务配置
- API Key
- 请求日志

点击“反代服务”一级导航时，默认进入“服务配置”页面。

#### CodeBuddy

- 账号池
- 模型管理

### 3.3 导航交互

以下规则已确认：

- 左侧导航默认收缩为仅图标的窄栏；
- 一级图标悬停时，从侧边弹出文字子菜单；
- 点击一级图标时固定打开对应子菜单，再次点击可关闭；
- 键盘焦点移动到一级图标时，同样打开对应子菜单；
- 按下 `Escape` 关闭当前子菜单；
- 子菜单只显示文字，不显示快捷键；
- 当前页面通过图标高亮、背景状态和顶部标题共同表达；
- 菜单与一级图标之间保留连续的可操作区域，避免鼠标移动经过间隙时菜单意外关闭；
- 菜单关闭可以有很短的容错延迟，但不能用延迟掩盖错误的热区设计；
- 窄栏图标必须提供可访问名称和悬停提示，避免用户只能依赖图形猜测页面含义。

视觉上保留 macOS 风味：使用安静的悬浮菜单、清晰的选中状态和轻微材质层级；行为上遵循 Windows 桌面习惯，确保点击、键盘焦点和窗口操作都完整可用。

### 3.4 顶部标题

使用自定义无边框标题栏。顶部空白区域作为窗口拖动区域，标题显示：

```text
CodeRelay / 总览
```

进入其他页面时，标题同步显示当前页面，例如：

```text
CodeRelay / CodeBuddy / 账号池
```

标题栏不放置服务状态、启动、停止或重启按钮。服务控制固定放在窗口底部状态栏右侧。

---

## 4. 窗口与整体布局

### 4.1 窗口结构

```text
┌──────────────────────────────────────────────────────────────┐
│ 自定义标题栏：CodeRelay / 当前页面                 窗口控制区 │
├──────┬───────────────────────────────────────────────────────┤
│      │ 页面标题、工具栏、内容区                              │
│ 窄栏 │                                                       │
│ 导航 │                                                       │
│      │                                                       │
├──────┴───────────────────────────────────────────────────────┤
│ 左侧辅助状态                         服务状态及启动/停止控制 │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 底部状态栏

底部状态栏右侧固定显示：

- 服务运行状态：运行中、已停止、启动中、停止中、错误；
- 启动、停止或重启操作；
- 服务正在启动或停止时显示进行中状态，并禁止产生互相冲突的操作；
- 状态文本必须和颜色同时存在，例如“运行中”，不能只用绿点表达状态；
- 服务控制在总览和服务配置页保持同一位置、同一文案和同一状态逻辑，避免用户在不同页面遇到不同操作含义。

总览页可以展示服务状态摘要，但底部状态栏是全局、持久的运行状态入口。

### 4.3 视觉基调

CodeRelay 采用“macOS 风味、Windows 行为”的视觉策略。macOS 风味体现在材质、空间、排版、圆角和动效节奏中，不体现在照搬 macOS 的标题栏布局、窗口按钮位置或系统交互。

- 默认采用黑、白、灰色体系；
- 灰色作为主要强调色；
- 绿色、黄色、红色只用于成功、警告和错误等语义状态；
- 使用微圆角卡片、分组容器和轻量浮层；
- 主内容区保持接近不透明，确保表格、日志和表单长时间阅读清晰；
- 标题栏、导航浮层、弹出菜单和非阻塞详情面板可以使用轻微透明和背景模糊；
- 阴影保持轻微，优先使用边界、留白和层级对比，而不是厚重投影；
- 不使用大面积渐变、装饰性插画或持续运动的背景；
- 不使用类似 macOS 的红黄绿窗口控制按钮作为主要窗口控制表达；Windows 窗口控制仍保持可识别、可贴靠和可最大化的桌面行为。

macOS 风味的核心感受是安静、清晰、轻盈和有空间感。不能通过降低对比度、隐藏关键操作或增加玻璃效果来换取所谓的高级感。

---

## 5. 总览页

软件启动后默认显示总览页。

### 5.1 页面目的

总览页用于回答四个问题：

1. 反代服务当前是否可用；
2. 最近一次运行处理了多少请求；
3. 账号池和调度器是否健康；
4. 用户是否需要立即处理异常。

### 5.2 内容区域

#### 区域一：服务状态区

显示：

- 运行中、已停止、启动中、停止中或错误；
- 已绑定 API Key 数量；
- 可用 CodeBuddy 账号数量；
- 最近启动时间、最近停止时间或最近一次错误时间；
- 需要处理的账号或配置异常数量。

不显示完整的本地服务地址、端口和默认 API Key。这些信息放在服务配置页和 API Key 页面。

#### 区域二：核心指标区

建议显示：

- 总请求数；
- 总 Token；
- 缓存命中率或缓存命中数量；
- Credit 消耗；
- 平均延迟；
- 统计时间范围。

服务停止后保留最近一次运行数据，并使用清晰的辅助文字标记，例如“上次运行：今天 14:20–16:05”。

#### 区域三：请求统计图表

包含按小时统计的“命中缓存”和“未命中”柱状图。

- 使用两种灰阶区分命中和未命中，避免依赖颜色；
- 鼠标悬停柱体时显示具体时间、请求数和比例；
- 横轴、纵轴和网格线保留；
- 没有请求数据时保留空图表容器、坐标轴和网格线，不显示“暂无请求数据”等大段提示文字；
- **建议**：无数据时可以保留一个低对比度的时间范围或“最近 24 小时”标签，让空图表看起来是有意设计的状态，而不是加载失败；
- 图表旁可显示请求日志摘要，例如最近错误类型、最近一次失败时间和失败请求数。

#### 区域四：调度状态卡片

显示：

- 当前调度策略；
- 会话亲和是否开启；
- 当前可用账号数；
- 冷却中的账号数；
- 最近一次账号切换原因；
- 当前是否存在没有可用账号的 API Key。

### 5.3 总览页不承担的功能

- 不直接编辑监听地址、端口和超时配置；
- 不直接展示完整默认 API Key；
- 不展示完整请求日志；
- 不将服务启动操作设计成自动发生的页面加载副作用。

---

## 6. 反代服务与服务配置

### 6.1 服务生命周期

- CodeRelay 启动后反代服务默认停止；
- 用户手动启动服务；
- 启动成功后显示绿色运行状态和“运行中”文本；
- 启动失败时在页面内显示错误状态，并发送 Windows 系统通知；
- 停止、重启和启动过程均显示明确的进行中状态；
- 服务停止后，历史统计数据仍保留在总览页。

### 6.2 服务配置项

服务配置页负责管理：

- 监听地址；
- 服务端口；
- 请求超时；
- 重试次数；
- 请求失败后的账号切换策略；
- 是否允许局域网访问；
- 兼容协议入口；
- 其他反代服务相关高级配置。

### 6.3 服务配置页布局

已确认采用“概要区 + 分组表单”的布局，而不是将所有输入框铺成一个长表单：

1. 顶部显示当前服务状态和最近一次错误；
2. “网络”分组放监听地址、端口和局域网访问；
3. “请求处理”分组放超时、重试和账号切换策略；
4. “协议兼容”分组放兼容协议入口；
5. 页尾或右上角提供保存配置和重启服务；
6. 每个字段旁边提供简短、具体的说明，复杂解释放入可展开的高级说明中。

保存配置不应默默重启服务。若修改需要重启，应在保存后明确提示“配置已保存，重启服务后生效”，并提供单独的重启操作。页面采用轻量分组、微圆角和适度留白，保持 macOS 风味，但不牺牲 Windows 表单的可读性。

---

## 7. CodeBuddy 账号池

### 7.1 账号池定位

账号池是 CodeRelay 的核心资源池，用于请求调度和模型调用。账号不应被视为单纯的登录记录，还需要显示健康状态、可用性、使用情况和绑定关系。

### 7.2 添加账号方式

以下方式全部支持，由用户选择，不强制单一认证流程：

1. **OAuth 或网页登录**；
2. **手动粘贴 Token**；
3. **导入已有账号配置文件**。

#### 7.2.1 OAuth 或网页登录

用户进入“添加账号”后选择“OAuth / 网页登录”。页面提供：

- “OAuth 授权”与“网页登录”的明确入口；
- 打开授权页面或登录页面的按钮；
- 当前认证步骤和等待状态；
- 授权成功后的账号信息预览；
- 取消认证和重新尝试；
- 认证失败的具体原因及下一步操作。

Token 不在普通界面中直接展示。网页登录或 OAuth 完成后，系统自动接收并保存账号凭据。

#### 7.2.2 手动粘贴 Token

用户选择“手动粘贴 Token”后：

- 使用多行文本框接收 Token；
- 提供“验证 Token”操作；
- 验证成功后显示账号标识、套餐或权限信息；
- 验证失败时指出格式错误、已失效、网络不可达或账号信息无法读取等原因；
- 成功保存后默认不回显完整 Token；
- **建议**：文本框支持粘贴后自动去除首尾空白，但不要擅自修改 Token 中间内容；
- **建议**：对 Token 输入区提供“显示/隐藏”控制，默认隐藏或使用安全输入样式。

#### 7.2.3 导入已有账号配置文件

用户选择“导入配置文件”后：

1. 打开 Windows 文件选择器；
2. 读取并解析支持的账号配置文件；
3. 在真正写入前展示导入预览；
4. 显示将导入的账号数量、重复账号、格式错误项和无法识别项；
5. 用户可以逐项取消选择后再导入；
6. 导入过程显示进度；
7. 导入结束后分别报告成功、跳过和失败的条目，并提供重试或查看原因的入口。

如果文件包含多个账号，不能只显示“导入成功”，应显示明确的结果摘要，例如“成功 8 个，重复 2 个，失败 1 个”。

### 7.3 添加账号统一面板

已确认采用一个中等宽度的统一模态面板，而不是三个完全独立的页面：

```text
┌─────────────────────────────────────────────┐
│ 添加 CodeBuddy 账号                    关闭 │
├──────────────┬──────────────────────────────┤
│ OAuth / 网页 │ 当前方式的说明和操作区域       │
│ 登录         │                              │
│              │                              │
│ 手动粘贴     │                              │
│ Token        │                              │
│              │                              │
│ 导入配置文件 │                              │
├──────────────┴──────────────────────────────┤
│                              取消    继续    │
└─────────────────────────────────────────────┘
```

左侧方法列表让用户随时切换认证方式，右侧只显示当前方法所需内容。这样既支持多种入口，又不会把所有字段同时堆在一个页面中。

OAuth 和网页登录可归为同一类“浏览器认证”，但右侧必须提供两个清晰的实际按钮，避免用户误以为二者是同一个流程。

面板采用 macOS 风味的轻量工作表表现：背景和页面保持空间关系，边界清晰，层级轻，不使用厚重遮罩或夸张缩放。它仍然必须支持 Windows 常见的关闭、Escape、Tab、Enter 和系统窗口焦点行为。

### 7.4 账号列表结构

账号数量较少时可以在顶部使用少量汇总卡片；主体默认采用高密度表格或可展开列表。账号详情放入展开行或右侧详情面板。账号池的核心任务是比较多个账号的状态，表格比重复大型卡片更适合横向比较。

已确认的建议字段：

- 账号名称或邮箱；
- 认证方式；
- 套餐或 tier；
- 健康状态；
- 可用额度或使用量；
- 最近成功请求时间；
- 连续失败次数；
- 绑定的 API Key 数量；
- 更多操作。

详细统计可以在展开行或右侧详情面板中显示：

- 请求数；
- Token 数；
- 成功、失败、取消、上游失败、流未完成统计；
- 最近失败原因；
- Token 更新时间；
- 调度优先级和权重。

这样可以保留信息密度，同时避免每个账号卡片都显示大量相同字段，导致页面缺少主次。

### 7.5 账号状态

账号状态至少包括：

- 可用；
- 验证中；
- 需要重新认证；
- 暂时冷却；
- 已禁用；
- 错误。

状态必须同时通过文本、图标或标记表达，不能只通过颜色表达。

### 7.6 删除账号与绑定关系

删除 CodeBuddy 账号前检查其绑定关系。

如果账号仍被 API Key 绑定：

- 弹窗列出绑定该账号的所有 Key；
- 说明删除后这些 Key 将无法使用该账号；
- 如果某个 Key 没有其他可用账号，请求可能返回错误；
- 用户必须二次确认后才能删除账号；
- 删除账号时不自动停用相关 API Key。

删除确认应当是具体的破坏性确认，而不是简单的“确定/取消”。确认按钮应明确写成“删除账号”，并把风险说明放在按钮上方。

---

## 8. API Key 管理

### 8.1 基本能力

每个 API Key 支持：

- 创建、启用、禁用和删除；
- 绑定可使用的 CodeBuddy 账号范围；
- 限制可使用的模型；
- 配置模型别名或模型前缀；
- 查看绑定关系和最近使用摘要。

API Key 默认采用 `sk-*` 命名格式。

### 8.2 账号绑定范围

至少支持：

- 使用全部 CodeBuddy 账号；
- 仅使用指定账号。

选择指定账号时提供账号列表和多选能力。

如果某个 API Key 绑定的账号全部不可用：

- 直接返回错误；
- 不使用未绑定的其他账号进行切换；
- 在 API Key 详情页和总览页显示该 Key 当前没有可用账号的状态。

### 8.3 UI 建议

- 列表页展示 Key 名称、状态、账号范围、模型范围、最近使用时间和请求摘要；
- 完整 Key 默认可复制，但复制动作应有即时反馈；
- 详情编辑建议使用右侧面板，减少页面跳转；
- 账号多选使用搜索、全选和已选数量，不把几十个账号平铺成长列表；
- 删除 Key 使用破坏性确认；禁用 Key 可以直接切换，但需要显示完成反馈。

---

## 9. 模型管理

模型管理用于查看可用模型、配置模型别名、启用或禁用模型，并表达模型能力。

建议显示：

- 模型名称；
- 能力标签，例如文本、视觉或其他协议能力；
- 当前可用状态；
- 最近同步时间；
- 别名和前缀配置；
- 关联账号或来源信息；
- 禁用原因或探测失败原因。

**建议**：模型列表使用表格，能力用小型标签表达；不要为每个模型使用大型卡片。模型管理的主要任务是查找、比较和修改配置，而不是浏览营销内容。

---

## 10. 请求日志

### 10.1 归属与保留

请求日志归属于“反代服务 → 请求日志”。

- 只保留最近 7 天；
- 导出范围是当前筛选后的日志，而不是全部历史日志；
- 仅支持导出 JSON；
- 导出内容为完整日志；
- API Key 默认显示完整内容，无需掩码。

### 10.2 日志列表建议字段

- 时间；
- 请求方法和路径；
- 模型；
- 使用的 CodeBuddy 账号；
- API Key；
- 请求状态；
- 总耗时；
- 输入和输出 Token；
- 缓存命中情况；
- Credit；
- 错误类型；
- 是否发生账号切换。

### 10.3 交互建议

- 顶部提供时间范围、状态、模型、账号、API Key 和错误类型筛选；
- 筛选结果数量和当前时间范围始终可见；
- 点击一行打开详情面板，不离开列表页面；
- 详情中显示请求元数据、调度过程、错误信息和完整响应摘要；
- 导出按钮明确写为“导出当前筛选结果 JSON”；
- 没有匹配结果时保留筛选条件，并提供清除筛选入口。

### 10.4 待确认的数据清理机制

**待确认**：7 天数据的清理时机，以及是否允许用户提前手动清理。

**建议**：

- 后台按固定周期自动清理超过 7 天的数据；
- 软件启动时执行一次兜底清理；
- 日志页提供“清理 7 天前日志”和“清理当前筛选结果”两个明确操作；
- 手动清理前显示条数和时间范围；
- 清理完成后显示删除数量；
- 清理操作不影响统计快照，除非产品最终决定统计完全依赖原始日志。

---

## 11. 系统托盘、通知与退出行为

### 11.1 托盘菜单

暂定菜单：

- 打开 CodeRelay；
- 启动反代服务；
- 停止反代服务；
- 退出 CodeRelay。

服务状态变化后，托盘菜单中的启动和停止操作应同步更新可用状态。

### 11.2 关闭窗口

点击关闭按钮时：

- 反代未运行：按默认退出行为处理；
- 反代正在运行：弹窗询问“最小化到托盘”或“继续退出”；
- 用户可以选择取消并返回主界面；
- 可勾选保存后续默认行为：
  - 以后默认最小化到托盘；
  - 以后默认退出。

### 11.3 继续退出

用户选择继续退出后：

1. 先停止反代服务；
2. 显示“正在停止反代服务…”；
3. 确认服务停止；
4. 退出 CodeRelay。

如果停止服务失败：

- 保留 CodeRelay 窗口；
- 提示服务可能仍在运行；
- 提供继续处理或强制退出选项；
- 强制退出前提示可能存在进程和端口占用风险。

### 11.4 Windows 系统通知

启动失败、停止失败或服务异常时发送 Windows 系统通知。通知内容必须包含：

- 发生了什么；
- 当前服务状态；
- 用户可以采取的动作。

通知不应只显示“操作失败”这种无法处理的问题描述。

---

## 12. Apple Design Skills 评审结论

Apple Design Skills 的核心并不是要求界面模仿 macOS，而是强调响应速度、空间关系、可预测反馈、层级、克制和可中断的交互。结合 Windows 桌面工具的使用场景，当前 CodeRelay 的整体方向是正确的，但需要对以下方面做明确约束。

### 12.1 当前方案中值得保留的部分

#### 目的性

总览、服务配置、账号池、模型管理、API Key 和请求日志的职责划分清晰，符合“每个页面回答一类问题”的原则。

#### 简洁而不是极简

黑白灰体系、窄栏导航和固定底部服务状态可以减少视觉噪声，同时保留高级用户需要的操作入口。这个方向适合 CodeRelay。

#### 用户控制权

服务不自动启动、账号认证方式由用户选择、关闭窗口时允许最小化到托盘或退出，这些设计都符合用户控制权原则。

#### 空间一致性

把服务状态和控制固定在底部状态栏，有利于形成稳定的空间记忆。只要服务配置页使用同一套状态逻辑，就能减少页面之间的认知切换。

### 12.2 已确认的设计约束

#### 1. 悬停导航增加键盘和点击路径

悬停菜单适合保持窄栏的高信息密度，但不能单独依赖悬停。CodeRelay 已确认采用以下方案：

- 悬停打开；
- 点击固定打开，再次点击关闭；
- 键盘聚焦时打开；
- `Escape` 关闭；
- 菜单与图标之间提供连续可操作区域；
- 使用短暂、克制的出现反馈，不使用明显的滑入动画。

#### 2. 账号池不把所有内容做成卡片

卡片用于顶部摘要或异常提示。账号主体采用表格或可展开列表，详情使用展开行或右侧面板，便于比较多个账号的健康状态、使用量和绑定关系。

#### 3. 透明材质限制在浮层

Apple Design Skills 提倡使用透明材质表达浮层层级，但 CodeRelay 不把所有区域做成玻璃效果。

**已确认限制**：

- 透明和模糊只用于标题栏、导航浮层、弹出菜单或非阻塞面板；
- 主内容区域使用接近不透明的背景，保证文字、表格和日志清晰；
- 不叠加多个半透明浅色表面；
- 在 Windows 的高对比度设置或用户减少透明效果时，自动切换为实色背景和清晰边框。

#### 4. 动效服务于状态和因果

CodeRelay 不使用大量手势驱动动效。Apple 的“可中断、从当前画面状态继续”原则只用于确实存在拖动或调整行为的控件：

- 导航浮层、菜单和详情面板：短距离、快速、无弹性的出现和关闭；
- 启动或停止服务：状态指示和按钮状态变化，不使用夸张弹跳；
- 可拖拽或可调整的面板：从当前屏幕位置继续动画，并继承拖动速度；
- 普通页面切换：优先淡入淡出或轻微位移，不使用大幅滑屏。

**已确认动效基线**：

- 按钮按下反馈：立即发生；
- 普通状态变化：约 120–180ms；
- 弹出面板：约 180–260ms；
- 默认不使用过冲和持续循环动画；
- 动效不能阻塞用户下一步输入。

#### 5. 文案直接说明结果

状态反馈使用具体文案，例如：

- “服务启动失败：端口 11435 已被占用”；
- “已导入 8 个账号，跳过 2 个重复账号”；
- “API Key 没有可用账号”；
- “正在停止反代服务…”。

避免只显示“失败”“处理中”“异常”等无法帮助用户判断下一步的词语。

#### 6. 颜色不承担唯一语义

运行状态、账号状态和日志结果同时使用文本、图标、形状或位置表达。红色只做强调，不作为唯一错误线索。

#### 7. 保留 Windows 的系统习惯

CodeRelay 在 Windows 上优先使用 `Segoe UI Variable` 或系统 UI 字体；Token、端口、路径、模型 ID 和 JSON 日志使用等宽字体。

自定义无边框窗口需要额外保证：

- 标题栏拖动区域不会覆盖按钮点击区域；
- 窗口最大化、恢复、贴靠和多显示器行为正常；
- 窗口控制按钮有足够的点击区域；
- 键盘焦点顺序稳定；
- Windows 高对比度模式下仍可辨识边界。

### 12.3 设计原则结论

CodeRelay 继续沿用“窄栏导航 + 大内容区 + 底部服务状态栏 + 黑白灰层级”的主方向，同时正式加入 macOS 风味的视觉语言。下一步重点是把这些规则落实到交互原型和组件规范中，而不是继续增加装饰。

### 12.4 macOS 风味的具体实现方向

macOS 风味定义为一种视觉和交互气质，而不是复制 macOS 的平台规则。以下特征已确认统一落实：

#### 空间与材质

- 标题栏和窄栏导航使用轻微半透明材质，主内容区保持接近不透明；
- 面板、弹出菜单和详情抽屉拥有清晰的层级关系，但阴影保持克制；
- 内容区域保留较充分的留白，使高信息密度不等于视觉拥挤；
- 浮层从其触发位置出现，保持来源和目标之间的空间关系；
- 不叠加多个透明表面，避免文字对比度下降。

#### 排版与控件

- 使用系统字体，标题采用较紧凑的字距和行高，正文保留舒适行高；
- 模型 ID、端口、Token 和日志数据使用等宽字体；
- 控件采用小圆角、明确焦点环和细边界；
- 按钮使用层级区分，不把所有操作都做成高对比度实心按钮；
- 主要操作保持一个明确的视觉重点，危险操作使用语义色和具体文案。

#### 交互节奏

- 按钮按下立即产生轻微视觉反馈；
- 菜单和面板快速、安静地出现，不做弹跳；
- 详情面板打开后可以继续操作列表，不阻塞非冲突任务；
- 需要确认的操作只用于删除账号、删除 Key、强制退出等不可逆或高风险行为；
- 服务启动、停止和导入过程持续显示状态，不用动画代替文字反馈。

#### Windows 边界

- 不使用 macOS 红黄绿窗口按钮；
- 不照搬 macOS 标题栏按钮位置或菜单栏结构；
- 保留 Windows 窗口贴靠、最大化、恢复、系统托盘和系统通知行为；
- 系统浏览器、文件选择器和通知优先使用 Windows 原生入口；
- 用户可以通过键盘完成主要任务。

### 12.5 设计原则结论

CodeRelay 当前的正式设计方向是：视觉上具有 macOS 风味，行为上符合 Windows 桌面习惯，信息组织上服务于高级用户的监控和配置任务。
---

## 13. 视觉基础建议

### 13.1 色彩层级

建议使用 4 个主要灰阶层级，而不是大量相近灰色：

- 应用背景：最深或最浅的基础色；
- 内容表面：与背景有轻微区分；
- 分组表面：用于输入区、弹窗和详情面板；
- 边界与辅助文字：保证足够对比度。

状态色只在状态点、状态标签、错误边框和系统通知中少量使用。

### 13.2 间距

建议建立固定间距刻度，例如：

- 4：图标和文字的紧密间距；
- 8：控件内部间距；
- 12：列表行和小分组间距；
- 16：卡片内边距和表单字段间距；
- 24：主要分组间距；
- 32：页面级留白。

页面外边距和内容间距应优先使用相对单位或统一设计变量，避免每个页面自行决定一套间距。

### 13.3 圆角和边界

- 输入框、按钮和列表行使用小圆角；
- 卡片和弹窗使用微圆角；
- 不同层级不要使用过多不同圆角值；
- 主要层级优先通过背景、边界和留白表达；
- 阴影只用于浮层和弹窗，主内容区不使用明显投影。

### 13.4 字体

- 页面标题：较大字号、紧凑字距和较短行高；
- 正文和表单说明：系统 UI 字体、舒适行高；
- 数据、端口、Token、路径、模型 ID：等宽字体；
- 不用字重和颜色同时过度强调同一信息；
- 用户提高系统文字大小时，布局应允许内容增长，不应依赖固定高度截断文字。

### 13.5 macOS 风味视觉变量

以下变量作为首版 UI 的统一起点，后续通过实际原型和 Windows 显示缩放测试调整：

- 间距以 4 为基础单位，主要使用 8、12、16、24 和 32；
- 控件圆角建议为 6px，卡片和分组容器建议为 8px，弹窗和详情面板建议为 10px；
- 控件最小点击区域建议不低于 32px，主要按钮和列表操作建议不低于 36px；
- 普通边界使用低对比度灰色，焦点边界使用清晰的灰色或系统强调色；
- 标题栏和浮层使用轻微透明与模糊，主内容表面保持接近不透明；
- 浮层阴影使用低扩散、低透明度效果，不使用厚重投影；
- 页面标题和分区标题使用略紧的字距，正文和说明文字保持接近默认字距；
- 关键状态使用“文字 + 图标 + 颜色”三种信号中的至少两种；
- 所有变量都必须在减少透明、高对比度和系统显示缩放下保持可读和可操作。

---

## 14. 交互反馈与无障碍

### 14.1 四类反馈

每个重要操作都应考虑：

- 状态反馈：正在启动、验证中、导入中；
- 完成反馈：服务已启动、账号已添加、配置已保存；
- 警告反馈：账号即将失效、Key 没有可用账号；
- 错误反馈：启动失败、Token 无效、配置文件解析失败。

### 14.2 减少动效

支持 Windows 或 WebView 可识别的减少动效设置：

- 将滑动、弹簧和大幅位移改为短暂淡入淡出；
- 移除弹性过冲和持续循环；
- 保留颜色、透明度和状态变化，以免反馈消失。

### 14.3 减少透明

当用户启用减少透明效果时：

- 半透明标题栏、导航浮层和弹窗切换为不透明背景；
- 移除背景模糊；
- 通过边界和阴影表达层级。

### 14.4 高对比度

当用户启用高对比度或提高对比度时：

- 使用接近不透明的表面；
- 显示明确边框；
- 增强焦点环和禁用状态的可辨识度；
- 不依赖灰色深浅区分所有状态。

### 14.5 键盘操作

至少支持：

- Tab 顺序访问标题栏、导航、页面控件和底部服务控制；
- Enter 或 Space 激活按钮；
- Escape 关闭菜单、弹窗和详情面板；
- 方向键在菜单、列表和多选控件中移动；
- 焦点位置始终可见。

---

## 15. 数据与安全要求

- Token、账号凭据和 API Key 仅在本地保存；
- 普通界面不回显完整上游 Token；
- API Key 按产品要求默认显示完整内容，复制时提供即时反馈；
- 导入配置文件在写入前先预览；
- 账号删除前检查 API Key 绑定；
- 日志只保留最近 7 天；
- 错误消息不得把上游 Token 或其他敏感凭据写入通知和普通日志；
- 配置修改、账号导入、账号删除和服务生命周期变化都应有可追溯的本地操作结果。

---

## 16. 待确认事项

以下内容需要在后续对话中继续确认：

1. 7 天日志是仅自动清理，还是同时允许用户手动清理；
2. 手动清理是否支持清理当前筛选结果；
3. 导入账号配置文件需要支持哪些具体格式和版本；
4. OAuth 与网页登录是使用系统浏览器、内置 WebView，还是两者均支持；
5. Token 验证失败时是否允许先保存为“待验证账号”；
6. CodeBuddy 国际站账号是否在首个版本继续隐藏；
7. 模型同步失败时的页面状态和本地缓存策略；
8. 是否需要内置测试对话入口；
9. 是否需要导出和导入 CodeRelay 自身的完整配置备份。

---

## 17. 分阶段交付建议

### 第一阶段：基础可用链路

- 自定义窗口和窄栏导航；
- 总览页基础结构；
- 账号池三种添加方式；
- 账号验证、列表和删除；
- API Key 创建和账号范围绑定；
- 服务配置、手动启动和停止；
- 底部状态栏和 Windows 通知。

### 第二阶段：调度与观测

- 多种调度策略；
- 会话亲和；
- 账号健康跟踪和冷却；
- 总览指标和按小时图表；
- 请求日志、筛选和 JSON 导出；
- 模型同步和模型管理。

### 第三阶段：完善体验

- 托盘和退出偏好；
- 绑定关系风险提示；
- 日志清理；
- 配置备份；
- 键盘操作、减少动效、减少透明和高对比度适配；
- 对话测试和更细粒度的错误诊断。

---

## 18. 验收重点

- 软件启动后默认进入总览页，反代服务保持停止；
- 用户可以从三种账号添加方式中任选一种，并能看到明确的进行中、成功和失败反馈；
- OAuth/网页登录、手动 Token、配置文件导入三条流程互不阻塞；
- API Key 只使用其绑定的账号范围；
- 删除仍被绑定的账号时必须显示绑定 Key 并要求二次确认；
- 启动服务失败时页面状态和 Windows 通知均出现；
- 关闭运行中的服务时能够正确执行最小化到托盘或停止后退出；
- 总览页在服务停止后保留最近一次运行数据，并标注数据时间范围；
- 请求日志只保留最近 7 天，并且导出内容严格对应当前筛选条件；
- 导航、弹窗、列表和服务控制支持键盘操作；
- 减少动效、减少透明和高对比度设置下，界面仍可使用且状态清晰。